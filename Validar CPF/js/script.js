function validar () {
    let form_cpf = window.document.getElementById('form_cpf')
    form_cpf.focus()
    let cpf = form_cpf.value
    let novo_cpf = 0
    for (let pos = 0; pos < cpf.length; pos++) {
        // if (cpf[pos] >= 0 && cpf[pos] <= 9) {
        //     novo_cpf += cpf[pos]
        // } else {
        //     return false
        // }
    }

}